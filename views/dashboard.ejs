<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/dashboard.css">
</head>
<body>
    <%- include('partials/_header') %>
<div class="dashboard-container">

    <!-- User's Favourite Cars -->
    <% cars.forEach(car => { %>
        <div class="car-entry">
            <img src="<%= car.image_url %>" alt="<%= car.name %>" class="car-image">
            <div class="car-details">
                <h3 class="car-model"><%= car.name %></h3>
                <!-- You can add the year and other details similarly -->
                <p class="car-price">$<%= car.price %></p>
                <!-- Delete Option (you might want to conditionally show this based on user rights or ownership) -->
                <button class="delete-car-btn">Delete</button>
            </div>
        </div>
    <% }) %>

</div>

</body>
</html>
